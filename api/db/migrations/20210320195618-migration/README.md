# Migration `20210320195618-migration`

This migration has been generated by patrick at 3/20/2021, 3:56:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Quest" (
    "id" TEXT NOT NULL,
    "contractAddress" TEXT NOT NULL,
    "methodName" TEXT NOT NULL,
    "purchaseBalance" TEXT,
    "domain" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "merchantId" TEXT NOT NULL,
    "triggerId" TEXT NOT NULL,
    "tokenId" TEXT,

    FOREIGN KEY ("merchantId") REFERENCES "Merchant"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("triggerId") REFERENCES "Trigger"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("tokenId") REFERENCES "Token"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Quest" ("id", "contractAddress", "methodName", "purchaseBalance", "domain", "name", "tokenId", "merchantId", "triggerId") SELECT "id", "contractAddress", "methodName", "purchaseBalance", "domain", "name", "tokenId", "merchantId", "triggerId" FROM "Quest";
DROP TABLE "Quest";
ALTER TABLE "new_Quest" RENAME TO "Quest";
CREATE UNIQUE INDEX "Quest_triggerId_unique" ON "Quest"("triggerId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "Hero_merchantId_unique" ON "Hero"("merchantId")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210320182837-migration..20210320195618-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = ["sqlite", "postgresql"]
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -20,9 +20,9 @@
   address String @id
   authDetail AuthDetail
   transactions Transaction[] @relation(name: "heroTransactions")
   quests Quest[]
-  merchantDetails Merchant?
+  merchantAccount Merchant?
   // activeQuest Quest @relation(name: "heroActiveQuest")
 }
 model Trigger {
@@ -38,10 +38,10 @@
   heroes Hero[] // Implicit m-n relationships don't require @relation
   heroesActive Hero[] @relation(name: "heroActiveQuest")
   contractAddress String
   methodName String
-  // purchaseToken Token @relation(name: "questPurchaseToken")
-  purchaseBalance String
+  purchaseToken Token? @relation(name: "questPurchaseToken")
+  purchaseBalance String?
   domain String
   name String
 }
@@ -55,8 +55,9 @@
 }
 model Merchant {
   id    String  @id @default(uuid())
+  owner Hero
   quests Quest[] @relation(name: "merchantQuests")
 }
 model Transaction {
```


