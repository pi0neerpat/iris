# Migration `20210321043013-migration`

This migration has been generated by patrick at 3/21/2021, 12:30:13 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Quest" (
    "id" TEXT NOT NULL,
    "contractAddress" TEXT NOT NULL,
    "abi" TEXT NOT NULL,
    "method" TEXT NOT NULL,
    "purchaseBalance" TEXT,
    "domain" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "merchantId" TEXT NOT NULL,
    "triggerId" TEXT NOT NULL,
    "tokenId" TEXT,

    FOREIGN KEY ("merchantId") REFERENCES "Merchant"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("triggerId") REFERENCES "Trigger"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("tokenId") REFERENCES "Token"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Quest" ("id", "contractAddress", "purchaseBalance", "domain", "name", "merchantId", "triggerId", "tokenId") SELECT "id", "contractAddress", "purchaseBalance", "domain", "name", "merchantId", "triggerId", "tokenId" FROM "Quest";
DROP TABLE "Quest";
ALTER TABLE "new_Quest" RENAME TO "Quest";
CREATE UNIQUE INDEX "Quest_triggerId_unique" ON "Quest"("triggerId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210320204951-migration..20210321043013-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = ["sqlite", "postgresql"]
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -43,9 +43,10 @@
   trigger Trigger @relation(name: "questTrigger")
   heroes Hero[] // Implicit m-n relationships don't require @relation
   heroesActive Hero[] @relation(name: "heroActiveQuest")
   contractAddress String
-  methodName String
+  abi String
+  method String
   purchaseToken Token? @relation(name: "questPurchaseToken")
   purchaseBalance String?
   domain String
   name String
```


