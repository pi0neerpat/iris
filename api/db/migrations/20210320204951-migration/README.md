# Migration `20210320204951-migration`

This migration has been generated by patrick at 3/20/2021, 4:49:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "AuthDetail" (
    "id" TEXT NOT NULL,
    "nonce" TEXT NOT NULL,
    "timestamp" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id")
)

CREATE TABLE "Hero" (
    "address" TEXT NOT NULL,
    "questId" TEXT,
    "authDetailId" TEXT NOT NULL,
    "merchantId" TEXT,

    FOREIGN KEY ("authDetailId") REFERENCES "AuthDetail"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("merchantId") REFERENCES "Merchant"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("questId") REFERENCES "Quest"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("address")
)

CREATE TABLE "Merchant" (
    "id" TEXT NOT NULL,
    PRIMARY KEY ("id")
)

CREATE TABLE "Trigger" (
    "id" TEXT NOT NULL,
    PRIMARY KEY ("id")
)

CREATE TABLE "Quest" (
    "id" TEXT NOT NULL,
    "contractAddress" TEXT NOT NULL,
    "methodName" TEXT NOT NULL,
    "purchaseBalance" TEXT,
    "domain" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "merchantId" TEXT NOT NULL,
    "triggerId" TEXT NOT NULL,
    "tokenId" TEXT,

    FOREIGN KEY ("merchantId") REFERENCES "Merchant"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("triggerId") REFERENCES "Trigger"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("tokenId") REFERENCES "Token"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

CREATE TABLE "Token" (
    "id" TEXT NOT NULL,
    "chainId" TEXT NOT NULL,
    "contractAddress" TEXT NOT NULL,
    "website" TEXT,
    "iconUrl" TEXT,
    PRIMARY KEY ("id")
)

CREATE TABLE "Transaction" (
    "id" TEXT NOT NULL,
    "timestamp" DATETIME NOT NULL,
    "blockNumber" TEXT NOT NULL,
    "status" TEXT NOT NULL,
    "questId" TEXT NOT NULL,
    "heroAddress" TEXT NOT NULL,

    FOREIGN KEY ("questId") REFERENCES "Quest"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("heroAddress") REFERENCES "Hero"("address") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

CREATE TABLE "_HeroToQuest" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL,

    FOREIGN KEY ("A") REFERENCES "Hero"("address") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("B") REFERENCES "Quest"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Hero_merchantId_unique" ON "Hero"("merchantId")

CREATE UNIQUE INDEX "Quest_triggerId_unique" ON "Quest"("triggerId")

CREATE UNIQUE INDEX "_HeroToQuest_AB_unique" ON "_HeroToQuest"("A", "B")

CREATE INDEX "_HeroToQuest_B_index" ON "_HeroToQuest"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210320204951-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,72 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = ["sqlite", "postgresql"]
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+model AuthDetail {
+  id        String   @id @default(uuid())
+  nonce     String
+  timestamp DateTime @default(now())
+}
+
+model Hero {
+  address String @id
+  authDetail AuthDetail
+  transactions Transaction[] @relation(name: "heroTransactions")
+  quests Quest[]
+  merchantAccount Merchant? //@relation(name: "heroMerchantAccount")
+  // activeQuest Quest @relation(name: "heroActiveQuest")
+}
+
+model Merchant {
+  id    String  @id @default(uuid())
+  owner Hero? //@relation(name: "heroMerchantAccount")
+  quests Quest[] @relation(name: "merchantQuests")
+}
+
+model Trigger {
+  id String @id @default(uuid())
+  quest Quest? @relation(name: "questTrigger")
+}
+
+model Quest {
+  id String @id @default(uuid())
+  merchant Merchant @relation(name: "merchantQuests")
+  transactions Transaction[] @relation(name: "questTransactions")
+  trigger Trigger @relation(name: "questTrigger")
+  heroes Hero[] // Implicit m-n relationships don't require @relation
+  heroesActive Hero[] @relation(name: "heroActiveQuest")
+  contractAddress String
+  methodName String
+  purchaseToken Token? @relation(name: "questPurchaseToken")
+  purchaseBalance String?
+  domain String
+  name String
+}
+
+model Token {
+  id        String   @id @default(uuid())
+  chainId String
+  contractAddress String
+  quests Quest[] @relation(name: "questPurchaseToken")
+  website String?
+  iconUrl String?
+}
+
+
+
+model Transaction {
+  id String @id
+  timestamp DateTime
+  blockNumber String
+  status String
+  quest Quest @relation(name: "questTransactions")
+  hero Hero @relation(name: "heroTransactions")
+}
```


